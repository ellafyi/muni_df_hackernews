{
  "name": "forum-newsreader",
  "version": "0.1.0",
  "private": true,
  "description": "A Hacker News-inspired Chromium extension for IS MU discussions.",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/clean.mjs && tsc && node scripts/copy-static.mjs",
    "package": "node scripts/clean.mjs && tsc && node scripts/copy-static.mjs && node scripts/package.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "@types/chrome": "^0.3.0",
    "archiver": "^8.0.0",
    "typescript": "^7.0.2"
  }
}
